#!/usr/bin/env dash

daemons() {
    printf 'gpg-agent\nssh-agent\n' | dmenu -p 'Daemon'
}

daemon="$(daemons)"
case "$daemon" in
    "gpg-agent")
        gpgconf --kill gpg-agent
        ;;
    "ssh-agent")
        keychain -k all
        ;;
    *)
        printf '%s\n' "$daemon"
        ;;
esac
