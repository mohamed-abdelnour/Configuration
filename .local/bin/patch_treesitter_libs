#!/usr/bin/env dash

treesitter_path="$HOME/.local/share/nvim/site/pack/packer/start/nvim-treesitter/parser"
lib_path="$HOME/.nix-profile/lib"

fd -I '\.so' "$treesitter_path" -x patchelf --set-rpath "$lib_path"
